# 仓库管理系统 (WMS) - 用户手册

## 系统概述

这是一个基于JavaFX和Socket通信的分布式仓库管理系统，支持多用户角色、完整的采购审批流程、出入库管理、报表统计等功能。

## 系统架构

### 技术栈
- **前端**: JavaFX
- **通信**: Socket (TCP/IP)
- **数据持久化**: MySQL数据库
- **并发**: 多线程 + 线程池

### 架构组成
1. **服务器端** (`WMSServer.java`): 
   - 监听端口8888
   - 处理多客户端并发连接
   - 统一管理业务数据
   - 记录操作日志

2. **客户端** (各角色界面):
   - 通过Socket连接服务器
   - 发送业务请求
   - 接收服务器响应

3. **数据模型**:
   - User - 用户
   - Product - 商品
   - PurchaseOrder - 采购订单
   - StockInRecord - 入库记录
   - StockOutRecord - 出库记录
   - OperationLog - 操作日志

## 用户角色与权限

### 1. 仓库管理员 (WAREHOUSE_ADMIN)
**用户名**: admin  
**密码**: admin123

**权限**:
- 管理所有用户
- 管理商品信息
- 查看所有出入库记录
- 查看系统日志
- 数据备份与恢复

### 2. 采购员 (PURCHASER)
**用户名**: purchaser  
**密码**: 123

**权限**:
- 创建采购订单
- 查看自己的采购订单
- 跟踪订单状态

### 3. 部门经理 (DEPARTMENT_MANAGER)
**用户名**: manager1  
**密码**: 123

**权限**:
- 审批金额 < 50,000元的采购订单
- 查看部门报表
- 查看库存信息

### 4. 总经理 (GENERAL_MANAGER)
**用户名**: general  
**密码**: 123

**权限**:
- 审批金额 ≥ 50,000元的采购订单
- 查看所有报表
- 查看全局数据

## 启动步骤

### 1. 启动服务器

#### 方式一：通过IDE运行
```
运行 WMSServer.java 的 main 方法
```

#### 方式二：通过命令行
```bash
cd "d:\Code\IDEA code\WMS"
javac -d target/classes -encoding UTF-8 src/main/java/com/_404/wms/network/WMSServer.java
java -cp target/classes com._404.wms.network.WMSServer
```

**服务器启动成功标志**:
```
WMS服务器已启动，监听端口: 8888
等待客户端连接...
示例数据初始化完成
```

### 2. 启动客户端

运行 `Launcher.java` 或 `LoginApplication.java`，进入登录界面。

## 功能模块详解

### 一、用户管理

#### 1.1 用户登录
- 输入用户名和密码
- 系统验证身份
- 根据角色跳转到对应工作台

#### 1.2 用户增删改查 (仅管理员)
- 添加新用户
- 编辑用户信息
- 禁用/启用用户
- 查看用户列表

### 二、商品管理

#### 2.1 商品信息维护
```java
// 商品属性
- 商品ID
- 商品名称
- 商品类别 (电子产品、办公用品、食品饮料等)
- 规格型号
- 单位
- 采购价格
- 销售价格
- 当前库存
- 最小库存 (安全库存)
- 最大库存
- 供应商
```

#### 2.2 库存查询
- 按类别筛选
- 按库存上下限筛选
- 低库存预警

#### 2.3 库存调整
- 盘点调整
- 损耗调整
- 记录调整日志

### 三、采购管理

#### 3.1 创建采购订单

**采购员操作流程**:
1. 选择供应商
2. 添加采购商品明细
3. 填写数量和单价
4. 系统自动计算总金额
5. 提交订单

**订单信息**:
```java
- 订单ID
- 采购员信息
- 供应商
- 商品明细 (商品、数量、单价、小计)
- 订单总金额
- 创建时间
- 备注
```

#### 3.2 采购审批流程

**审批规则**:
- **金额 < 50,000元**: 部门经理审批
- **金额 ≥ 50,000元**: 总经理审批

**订单状态流转**:
```
草稿 → 待部门经理审批 → 待总经理审批 → 已批准 → 运输中 → 已到货 → 已完成
                ↓                ↓
              已退回          已退回
```

**审批操作**:
1. 查看待审批订单列表
2. 选择订单查看详情
3. 填写审批意见
4. 点击"通过"或"退回"

#### 3.3 订单追踪
- 查看订单状态
- 查看审批历史
- 到货登记

### 四、出入库管理

#### 4.1 入库管理

**入库流程**:
1. 选择采购订单 (或手动输入)
2. 扫描/选择商品
3. 输入入库数量
4. 生成批次号
5. 选择仓库位置
6. 确认入库

**入库记录**:
```java
- 记录ID
- 关联订单ID
- 商品信息
- 入库数量
- 批次号
- 仓库位置
- 操作员
- 入库时间
```

#### 4.2 出库管理

**出库流程**:
1. 扫描/选择商品
2. 输入出库数量
3. 填写领用人信息
4. 填写出库原因/用途
5. 系统检查库存
6. 确认出库

**出库记录**:
```java
- 记录ID
- 商品信息
- 出库数量
- 领用人
- 领用部门
- 出库原因
- 操作员
- 出库时间
```

#### 4.3 库存预警

**自动预警触发条件**:
- 当前库存 ≤ 最小库存

**预警通知方式**:
- 界面提示
- 预警列表
- 发送通知给相关人员

### 五、报表与统计

#### 5.1 库存报表

**报表内容**:
- 商品分类统计
- 库存价值统计
- 库存周转率
- 滞销商品分析
- 库存预警统计

**筛选条件**:
- 时间范围
- 商品类别
- 库存状态

#### 5.2 采购报表

**报表内容**:
- 采购订单统计
- 采购金额趋势
- 供应商分析
- 到货率统计
- 采购成本分析

**可视化图表**:
- 柱状图: 采购成本趋势
- 折线图: 库存价值变化
- 饼图: 商品类别占比

#### 5.3 报表导出

**支持格式**:
- Excel (.xlsx)
- CSV (.csv)
- PDF (.pdf)

**导出步骤**:
1. 设置筛选条件
2. 生成报表
3. 点击"导出报表"
4. 选择格式和保存位置

### 六、系统安全与日志

#### 6.1 操作日志

**日志内容**:
```java
- 日志ID
- 操作用户
- 操作类型 (登录、创建、更新、删除、审批等)
- 操作模块 (用户管理、商品管理、采购管理等)
- 操作详情
- IP地址
- 操作时间
- 成功/失败状态
- 错误信息
```

**日志查询**:
- 按用户查询
- 按时间范围查询
- 按操作类型查询
- 按模块查询

#### 6.2 数据备份

**备份操作**:
1. 进入系统设置
2. 点击"数据备份"
3. 选择备份路径
4. 确认备份

**备份内容**:
- 用户数据
- 商品数据
- 采购订单
- 出入库记录
- 操作日志

#### 6.3 数据恢复

**恢复操作**:
1. 进入系统设置
2. 点击"数据恢复"
3. 选择备份文件
4. 确认恢复
5. 系统重新加载数据

## Socket通信协议

### 消息类型 (MessageType)

```java
// 认证相关
LOGIN_REQUEST       - 登录请求
LOGIN_RESPONSE      - 登录响应
LOGOUT              - 登出

// 用户管理
USER_ADD            - 添加用户
USER_UPDATE         - 更新用户
USER_DELETE         - 删除用户
USER_QUERY          - 查询用户
USER_LIST           - 用户列表

// 商品管理
PRODUCT_ADD         - 添加商品
PRODUCT_UPDATE      - 更新商品
PRODUCT_DELETE      - 删除商品
PRODUCT_QUERY       - 查询商品
PRODUCT_LIST        - 商品列表
PRODUCT_STOCK_ALERT - 库存预警

// 采购管理
PURCHASE_ORDER_CREATE  - 创建采购订单
PURCHASE_ORDER_UPDATE  - 更新采购订单
PURCHASE_ORDER_APPROVE - 审批通过
PURCHASE_ORDER_REJECT  - 审批退回
PURCHASE_ORDER_QUERY   - 查询订单
PURCHASE_ORDER_LIST    - 订单列表

// 出入库管理
STOCK_IN         - 入库
STOCK_OUT        - 出库
STOCK_QUERY      - 库存查询
STOCK_IN_LIST    - 入库记录列表
STOCK_OUT_LIST   - 出库记录列表

// 报表
REPORT_INVENTORY - 库存报表
REPORT_PURCHASE  - 采购报表
REPORT_EXPORT    - 导出报表

// 日志
LOG_QUERY        - 查询日志
LOG_LIST         - 日志列表

// 系统
BACKUP           - 数据备份
RESTORE          - 数据恢复
HEARTBEAT        - 心跳
ERROR            - 错误
```

### 消息格式

```java
public class Message {
    MessageType type;    // 消息类型
    String sender;       // 发送者ID
    Object data;         // 消息数据
    boolean success;     // 操作是否成功
    String message;      // 提示信息
    long timestamp;      // 时间戳
}
```

### 通信示例

#### 登录
```java
// 客户端发送
Message request = new Message(LOGIN_REQUEST);
Map<String, String> credentials = new HashMap<>();
credentials.put("username", "admin");
credentials.put("password", "admin123");
request.setData(credentials);

// 服务器响应
Message response = new Message(LOGIN_RESPONSE);
response.setSuccess(true);
response.setData(userObject);
response.setMessage("登录成功");
```

#### 创建采购订单
```java
// 客户端发送
PurchaseOrder order = new PurchaseOrder();
// ... 设置订单信息
Message request = new Message(PURCHASE_ORDER_CREATE, order);

// 服务器响应
Message response = new Message(PURCHASE_ORDER_CREATE);
response.setSuccess(true);
response.setData(savedOrder);
response.setMessage("订单创建成功");
```

## 数据存储

### 数据库表结构
所有数据存储在MySQL数据库中：

| 表名                 | 说明         |
| -------------------- | ------------ |
| users                | 用户信息     |
| products             | 商品信息     |
| purchase_orders      | 采购订单     |
| purchase_order_items | 采购订单明细 |
| stock_in_records     | 入库记录     |
| stock_out_records    | 出库记录     |
| operation_logs       | 操作日志     |

### 数据库配置
编辑 `src/main/resources/config.ini`：
```ini
[Mysql]
Url = jdbc:mysql://127.0.0.1:3306/
User = root
Passwd = your_password
Schema = wms_db
PoolSize = 10
```

## 常见问题

### Q1: 无法连接到服务器？
**A**: 检查以下几点：
1. 服务器是否已启动
2. 端口8888是否被占用
3. 防火墙是否允许连接
4. 客户端配置的服务器地址是否正确 (默认localhost)

### Q2: 登录失败？
**A**: 
1. 确认用户名和密码正确
2. 检查用户是否被禁用
3. 查看服务器日志是否有错误信息

### Q3: 数据丢失？
**A**:
1. 检查MySQL数据库连接是否正常
2. 确认数据库服务已启动
3. 检查 config.ini 配置是否正确

### Q4: 审批按钮点击无反应？
**A**:
1. 检查是否选中了待审批任务
2. 检查网络连接是否正常
3. 查看是否有权限审批该订单

## 系统扩展

### 添加新角色
1. 在 `User.UserRole` 枚举中添加新角色
2. 创建对应的FXML界面
3. 在 `LoginController.openRoleBasedInterface()` 中添加路由
4. 在服务器端添加权限验证

### 添加新功能模块
1. 创建对应的实体类
2. 在 `dao/` 中创建新的DAO接口和实现
3. 在 `DataService` 中添加数据管理方法
4. 在 `Message.MessageType` 中添加消息类型
5. 在 `WMSServer` 中添加消息处理方法
6. 创建客户端界面和控制器

## 技术支持

- **开发团队**: _404 Team
- **项目位置**: d:\Code\IDEA code\WMS
- **版本**: 2.0.0
- **更新日期**: 2025-12-04

---

## 快速开始指南

1. **配置数据库**
   ```
   编辑 src/main/resources/config.ini 配置MySQL连接
   ```

2. **启动服务器**
   ```
   运行 WMSServer.java
   ```

3. **启动客户端**
   ```
   运行 Launcher.java
   ```

4. **使用管理员账号登录**
   ```
   用户名: admin
   密码: admin123
   ```

4. **体验功能**
   - 查看示例商品
   - 创建采购订单
   - 使用经理账号审批订单
   - 查看报表和日志

祝您使用愉快！
