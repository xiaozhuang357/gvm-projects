# 快速启动指南

## Windows 用户

### 方式一：使用批处理脚本（推荐）

1. **快速启动**
   ```
   双击 quick_start.bat
   选择操作：
   1 - 仅启动服务器
   2 - 仅启动客户端
   3 - 同时启动服务器和客户端
   ```

2. **单独启动服务器**
   ```
   双击 start_server.bat
   ```

3. **单独启动客户端**
   ```
   在IDE中运行 Launcher.java
   或使用命令: mvnw javafx:run
   ```

### 方式二：使用IDE（推荐开发者）

1. **启动服务器**
   - 打开 `WMSServer.java`
   - 右键 → Run 'WMSServer.main()'
   - 等待显示 "WMS服务器已启动"

2. **启动客户端**
   - 打开 `Launcher.java`
   - 右键 → Run 'Launcher.main()'
   - 进入登录界面

## Linux/Mac 用户

### 启动服务器
```bash
cd /path/to/WMS
javac -d target/classes -encoding UTF-8 -cp src/main/java \
    src/main/java/com/_404/wms/model/*.java \
    src/main/java/com/_404/wms/network/*.java \
    src/main/java/com/_404/wms/service/*.java

java -cp target/classes com._404.wms.network.WMSServer
```

### 启动客户端
```bash
./mvnw javafx:run
```

## 测试账号

启动后使用以下账号登录：

| 角色       | 用户名    | 密码     |
| ---------- | --------- | -------- |
| 仓库管理员 | admin     | admin123 |
| 部门经理   | manager1  | 123      |
| 总经理     | general   | 123      |
| 采购员     | purchaser | 123      |

## 验证系统运行

1. **服务器启动成功标志**
   ```
   WMS服务器已启动，监听端口: 8888
   等待客户端连接...
   示例数据初始化完成
   ```

2. **客户端连接成功**
   - 登录界面出现
   - 输入账号密码
   - 能够正常登录进入工作台

3. **测试功能**
   - 使用 manager1/123 登录
   - 查看报表管理界面
   - 切换到审批中心
   - 查看待审批任务

## 常见问题

### Q: 端口8888被占用？
```bash
# Windows
netstat -ano | findstr 8888
taskkill /F /PID <进程ID>

# Linux/Mac
lsof -i :8888
kill -9 <进程ID>
```

### Q: 客户端无法连接？
- 确认服务器已启动
- 检查防火墙设置
- 查看服务器控制台是否有错误

### Q: JavaFX运行错误？
- 确认JDK版本 ≥ 17
- 确认已安装JavaFX
- 使用 `mvnw javafx:run` 而不是 `java -jar`

## 开发模式

推荐使用IDE（IntelliJ IDEA / Eclipse）：
1. 导入Maven项目
2. 等待依赖下载完成
3. 运行WMSServer（服务器）
4. 运行Launcher（客户端）

## 下一步

- 📖 查看 [系统使用手册.md](系统使用手册.md) 了解详细功能
- 📝 查看 [项目总结.md](项目总结.md) 了解技术实现
- 🚀 开始使用系统，探索各种功能

---

祝您使用愉快！

如有问题，请查阅文档或联系开发团队。
