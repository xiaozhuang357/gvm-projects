# WMS 仓库管理系统 - 项目总结

## 已完成功能

### ✅ 核心架构

#### 1. 数据模型层 (model包)
已创建完整的实体类：

- **User.java** - 用户实体
  - 支持4种角色：仓库管理员、采购员、部门经理、总经理
  - 包含用户基本信息、角色、部门、权限等
  
- **Product.java** - 商品实体
  - 商品基本信息
  - 库存管理（当前库存、最小库存、最大库存）
  - 库存预警功能
  
- **PurchaseOrder.java** - 采购订单实体
  - 订单基本信息
  - 订单明细（OrderItem内部类）
  - 多状态流转（草稿→审批→批准→运输→完成）
  - 自动计算总金额
  - 支持逐级审批逻辑
  
- **StockInRecord.java** - 入库记录
  - 入库信息登记
  - 批次管理
  - 关联采购订单
  
- **StockOutRecord.java** - 出库记录
  - 出库信息登记
  - 领用人信息
  - 出库原因追踪
  
- **OperationLog.java** - 操作日志
  - 记录所有用户操作
  - 支持多种操作类型
  - 包含时间戳、操作详情、成功状态

#### 2. 数据库层（分层架构）

##### 配置层 (config/)
- **ConfigManager.java** - INI配置文件读取
- **DatabaseConfig.java** - 数据库连接配置封装

##### 数据库连接层 (db/)
- **DatabaseManager.java** - 数据库管理器（单例模式）
  - 初始化数据库连接
  - 自动创建表结构
  - 管理连接池生命周期
  
- **connection/ConnectionPool.java** - 连接池接口
- **connection/MysqlConnectionPool.java** - MySQL连接池实现
  - 连接池管理
  - 健康检查
  - 自动重连
  
- **util/DbUtils.java** - 数据库工具类
  - 安全关闭资源
  - 参数设置辅助

##### DAO层 (dao/)
- **BaseDao.java** - DAO基础接口
- **DaoFactory.java** - DAO工厂（单例模式）
- **UserDao.java** - 用户数据访问接口
- **ProductDao.java** - 商品数据访问接口
- **PurchaseOrderDao.java** - 采购订单数据访问接口
- **StockInRecordDao.java** - 入库记录数据访问接口
- **StockOutRecordDao.java** - 出库记录数据访问接口
- **OperationLogDao.java** - 操作日志数据访问接口

##### DAO实现层 (dao/impl/)
- **AbstractDao.java** - DAO抽象基类，提供通用方法
- 各实体的DAO实现类

#### 3. 网络通信层 (network包)

- **Message.java** - 消息封装类
  - 统一的消息格式
  - 30+种消息类型
  - 支持成功/失败响应
  - 包含时间戳和发送者信息
  
- **WMSServer.java** - 服务器端
  - 监听端口8888
  - 支持多客户端并发连接
  - 使用线程池处理请求
  - 完整的消息路由和处理
  - 内置用户认证
  - 处理以下业务：
    - 用户登录/登出
    - 用户管理（增删改查）
    - 商品管理（增删改查）
    - 采购订单（创建、审批、退回）
    - 出入库操作
    - 日志查询
  
- **SocketClient.java** - 客户端工具类
  - 自动连接服务器
  - 发送/接收消息
  - 心跳保活
  - 异步消息处理
  - 支持同步和异步调用

#### 4. 业务逻辑层 (service包)

- **DataService.java** - 数据服务类
  - 封装所有DAO操作
  - 提供业务逻辑门面
  - 数据库操作统一入口
  - 支持以下功能：
    - 用户管理和认证
    - 商品管理和查询
    - 采购订单管理
    - 出入库记录管理
    - 操作日志管理
    - 低库存商品查询
    - 按状态查询订单

#### 5. 界面控制层

- **LoginController.java** - 登录控制器
  - Socket连接管理
  - 用户认证
  - 角色识别
  - 根据角色跳转不同界面
  - 全局SocketClient单例
  
- **ManagerController.java** - 经理工作台控制器
  - 用户信息显示
  - 从服务器加载数据
  - 审批中心功能
  - 与服务器实时交互
  - 支持通过/退回审批
  - 自动刷新待审批任务

- **PurchaserController.java** - 采购员工作台控制器
  - 创建采购订单
  - 查看订单列表
  - 产品目录浏览

- **WarehouseAdminController.java** - 仓库管理员工作台控制器
  - 产品管理
  - 库存管理（入库/出库）
  - 用户管理
  - 报表统计

### ✅ 已实现界面

#### 1. 登录界面 (login.fxml)
- 用户名密码输入
- 登录验证
- 连接服务器
- 角色识别

#### 2. 经理工作台 (manager.fxml)
- **顶部标题栏**：
  - 用户信息显示
  - 系统标题
  - 退出登录按钮
  
- **审批中心界面**：
  - 待审批任务列表
  - 任务详情显示
  - 审批操作按钮（通过/退回）
  - 待处理任务计数器

#### 3. 采购员工作台 (purchaser.fxml)
- 创建采购订单
- 我的订单列表
- 产品目录

#### 4. 仓库管理员工作台 (warehouse_admin.fxml)
- 产品管理
- 库存管理（入库/出库/记录查看）
- 用户管理
- 报表统计

### ✅ 核心业务流程

#### 1. 用户认证流程
```
用户输入账号密码 
→ 客户端连接服务器 
→ 发送登录请求 
→ 服务器验证（通过UserDao查询数据库）
→ 返回用户信息 
→ 记录登录日志 
→ 跳转对应角色界面
```

#### 2. 采购审批流程
```
采购员创建订单 
→ 计算总金额 
→ 自动判断审批级别
   ├─ < 50000元 → 部门经理审批
   └─ ≥ 50000元 → 总经理审批
→ 经理查看待审批订单 
→ 审批通过/退回 
→ 更新订单状态 
→ 记录审批日志 
→ 通知相关人员
```

#### 3. 数据持久化流程
```
业务操作 
→ 调用DataService 
→ 通过DAO层操作数据库 
→ 数据存储到MySQL
```

## 数据存储结构

所有数据存储在MySQL数据库中：

| 表名                 | 说明         |
| -------------------- | ------------ |
| users                | 用户信息     |
| products             | 商品信息     |
| purchase_orders      | 采购订单     |
| purchase_order_items | 采购订单明细 |
| stock_in_records     | 入库记录     |
| stock_out_records    | 出库记录     |
| operation_logs       | 操作日志     |

## 测试账号

| 角色       | 用户名    | 密码     | 用户ID |
| ---------- | --------- | -------- | ------ |
| 仓库管理员 | admin     | admin123 | U001   |
| 部门经理   | manager1  | 123      | U002   |
| 总经理     | general   | 123      | U003   |
| 采购员     | purchaser | 123      | U004   |

## 技术亮点

### 1. 分层数据库架构
- 配置层、连接层、DAO层、服务层分离
- 使用工厂模式管理DAO实例
- 连接池优化数据库性能

### 2. Socket长连接
- 客户端与服务器保持长连接
- 减少握手开销
- 支持服务器主动推送
- 心跳保活机制

### 3. 线程池管理
- 使用ExecutorService管理连接线程
- 自动扩展线程池大小
- 优化并发性能

### 4. 数据安全
- 操作日志完整记录
- 角色权限控制
- 数据库事务支持

### 5. 异步处理
- UI操作不阻塞
- 使用Platform.runLater()更新界面
- 网络请求异步执行

## 目录结构

```
src/main/java/com/_404/wms/
├── config/             # 配置管理
│   ├── ConfigManager.java
│   └── DatabaseConfig.java
├── db/                 # 数据库层
│   ├── DatabaseManager.java
│   ├── connection/
│   │   ├── ConnectionPool.java
│   │   └── MysqlConnectionPool.java
│   └── util/
│       └── DbUtils.java
├── dao/                # 数据访问层
│   ├── BaseDao.java
│   ├── DaoFactory.java
│   ├── *Dao.java (各实体DAO接口)
│   └── impl/          # DAO实现
├── model/              # 实体类
├── network/            # 网络层
│   ├── Message.java
│   ├── SocketClient.java
│   └── WMSServer.java
├── service/            # 业务逻辑层
│   └── DataService.java
├── *Controller.java    # JavaFX控制器
└── Launcher.java       # 程序入口

src/main/resources/
├── config.ini          # 数据库配置
└── com/_404/wms/
    ├── login.fxml
    ├── manager.fxml
    ├── purchaser.fxml
    └── warehouse_admin.fxml
```

## 运行说明

### 第一步：配置数据库
编辑 `src/main/resources/config.ini`：
```ini
[Mysql]
Url = jdbc:mysql://127.0.0.1:3306/
User = root
Passwd = your_password
Schema = wms_db
PoolSize = 10
```

### 第二步：启动服务器
```bash
运行 WMSServer.java
等待显示 "WMS服务器已启动，监听端口: 8888"
```

### 第三步：启动客户端
```bash
运行 Launcher.java
```

### 第四步：登录测试
使用测试账号登录，体验各角色功能

## 项目亮点

✅ **分层架构设计**：配置层、数据库层、DAO层、服务层、控制层清晰分离  
✅ **Socket通信**：真正的客户端-服务器架构  
✅ **多用户角色**：权限控制完善  
✅ **业务流程完整**：采购审批流程真实可用  
✅ **数据库持久化**：使用MySQL存储所有数据  
✅ **连接池优化**：高效的数据库连接管理  
✅ **操作日志**：所有操作可追溯  
✅ **现代化界面**：使用JavaFX，界面美观  
✅ **可扩展性强**：易于添加新功能  

## 项目文档

- `README.md` - 项目说明
- `系统使用手册.md` - 详细使用说明
- `系统运行逻辑说明.md` - 系统架构说明
- `docs/database.md` - 数据库文档
- `项目总结.md` - 本文档

---

**开发团队**: _404 Team  
**版本**: v2.0.0

