# WMS 仓储管理系统 - 系统运行逻辑说明文档

## 1. 系统架构概述

本系统采用标准的 **C/S (Client/Server)** 架构，分为客户端和服务器端两个主要部分。

*   **客户端 (Client)**: 基于 JavaFX 开发的桌面应用程序，负责用户交互、数据展示和操作请求发送。
*   **服务器端 (Server)**: 基于 Java Socket 开发的后台服务，负责处理客户端请求、业务逻辑处理、数据持久化。
*   **数据库 (Database)**: 使用 MySQL 8.0 存储核心数据（目前主要是用户信息），部分业务数据（商品、订单等）采用文件序列化方式存储（混合存储模式）。

### 1.1 技术栈
*   **编程语言**: Java 17+
*   **GUI框架**: JavaFX 21
*   **网络通信**: Java Socket (TCP/IP)
*   **数据库**: MySQL 8.0 (JDBC)
*   **构建工具**: Maven

---

## 2. 启动流程

### 2.1 服务器端启动 (`WMSServer.java`)
1.  **初始化**:
    *   创建 `DataService` 实例，负责加载和管理所有业务数据。
    *   `DataService` 初始化时会连接 MySQL 数据库 (`MysqlMgr`) 并加载本地序列化数据文件（如 `products.dat`）。
    *   初始化线程池 `ExecutorService` 用于并发处理客户端连接。
2.  **监听端口**:
    *   在端口 `8888` 上创建 `ServerSocket`。
    *   进入无限循环，等待客户端连接 (`serverSocket.accept()`)。
3.  **处理连接**:
    *   当有新客户端连接时，创建一个 `ClientHandler` 线程。
    *   `ClientHandler` 负责该客户端的所有后续通信（接收请求 -> 处理业务 -> 发送响应）。

### 2.2 客户端启动 (`Launcher.java` / `LoginApplication.java`)
1.  **启动界面**: 加载 `login.fxml` 显示登录窗口。
2.  **建立连接**:
    *   在 `LoginController` 初始化时，尝试连接服务器 (`127.0.0.1:8888`)。
    *   创建 `SocketClient` 实例，维护与服务器的长连接。
    *   启动后台监听线程，实时接收服务器推送的消息（如库存预警）。

---

## 3. 网络通信协议

系统使用自定义的 `Message` 类进行通信，基于 Java 对象序列化 (`ObjectOutputStream`/`ObjectInputStream`)。

### 3.1 消息结构 (`Message.java`)
*   `type`: 消息类型 (枚举 `MessageType`，如 `LOGIN_REQUEST`, `PRODUCT_LIST`)。
*   `sender`: 发送者ID。
*   `data`: 消息载荷 (Object)，可以是实体对象、列表或简单的字符串。
*   `success`: 操作状态 (boolean)。
*   `message`: 提示信息 (String)。

### 3.2 通信模式
*   **请求-响应 (Request-Response)**: 客户端发送请求（如查询商品），服务器处理后返回结果。这是最主要的交互方式。
*   **服务器推送 (Server Push)**: 服务器主动向客户端发送消息（如库存不足预警），客户端监听线程接收并更新 UI。

---

## 4. 核心业务逻辑

### 4.1 用户认证与权限管理
*   **登录**:
    *   客户端发送 `LOGIN_REQUEST` (包含用户名/密码)。
    *   服务器通过 `MysqlMgr` 查询 `users` 表进行验证。
    *   验证成功后，返回包含用户角色 (`UserRole`) 的 `User` 对象。
*   **权限控制**:
    *   **仓库管理员 (WAREHOUSE_ADMIN)**: 用户管理、商品管理、出入库操作、日志查看。
    *   **采购员 (PURCHASER)**: 采购申请、查看商品。
    *   **部门经理 (DEPARTMENT_MANAGER)**: 审批 5万元以下的采购订单。
    *   **总经理 (GENERAL_MANAGER)**: 审批 5万元及以上的采购订单。

### 4.2 数据存储策略 (混合模式)
*   **MySQL 数据库**:
    *   存储 **用户信息 (`users` 表)**。
    *   利用 `MysqlMgr` 和 `MysqlPool` 进行连接池管理，提高并发性能。
*   **文件序列化存储 (`wms_data/` 目录)**:
    *   存储 **商品 (`products.dat`)**、**采购订单 (`orders.dat`)**、**出入库记录**、**日志**。
    *   `DataService` 在启动时从文件加载到内存 (`ConcurrentHashMap`)，在数据变更时异步或同步保存回文件。
    *   *注意*: 这种方式适合中小规模数据，便于快速部署，但数据一致性依赖于文件系统的可靠性。

### 4.3 采购业务流程
1.  **创建订单**: 采购员提交订单 -> 状态变为 `PENDING_SUBMIT` (待提交) -> 提交后变为 `PENDING_DEPT_APPROVAL` (待部门经理审批)。
2.  **部门经理审批**:
    *   若金额 < 50,000: 批准 -> 状态 `APPROVED`。
    *   若金额 >= 50,000: 批准 -> 状态 `PENDING_GENERAL_APPROVAL` (待总经理审批)。
    *   驳回 -> 状态 `REJECTED`。
3.  **总经理审批**: 针对大额订单进行最终审批。
4.  **入库**: 订单批准后，仓库管理员进行入库操作 -> 增加商品库存 -> 生成入库记录 -> 订单状态 `COMPLETED`。

### 4.4 库存管理
*   **出入库**: 增减 `Product` 对象的 `currentStock` 字段。
*   **库存预警**:
    *   每次库存变动时，服务器检查 `currentStock <= minStock`。
    *   如果触发预警，服务器通过 `Socket` 向所有在线的 **仓库管理员** 发送 `PRODUCT_STOCK_ALERT` 消息。

---

## 5. 目录结构说明

```
src/main/java/com/_404/wms/
├── databases/          # 数据库层
│   ├── config/         # 配置读取 (config.ini)
│   ├── mysql/          # MySQL连接池与操作管理
│   └── Main.java       # 数据库测试入口
├── model/              # 实体类 (User, Product, Order...)
├── network/            # 网络层
│   ├── Message.java    # 通信协议包
│   ├── SocketClient.java # 客户端Socket封装
│   └── WMSServer.java  # 服务器主程序
├── service/            # 业务逻辑层
│   └── DataService.java # 数据处理核心 (CRUD, 文件持久化)
├── *.java              # JavaFX 控制器 (Login, Manager, etc.)
└── Launcher.java       # 程序启动入口
```

## 6. 常见问题排查

1.  **数据库连接失败**:
    *   检查 `src/main/resources/config.ini` 中的 URL、User、Passwd 是否正确。
    *   确保 MySQL 服务已启动且 `wms_db` 数据库已创建。
2.  **端口被占用**:
    *   服务器默认监听 `8888` 端口。若启动失败，请检查是否有其他进程占用该端口，或在 `WMSServer.java` 中修改 `PORT` 常量。
3.  **中文乱码**:
    *   项目统一使用 `UTF-8` 编码。确保数据库连接字符串包含 `characterEncoding=utf8` (虽然 JDBC 8.0 通常自动处理)。
